## FreeCAD scripting environment

Ubuntu environment for writing and testing FreeCAD scripts

### Run Docker container  
- `bash buildDocker.sh`  
OR  
- `docker build .`  
- `docker run -it <imageID>`
